apiVersion: v1
kind: Service
metadata:
  name: {{ include "asd.fullname" . }}
  labels:
    {{- include "asd.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "asd.selectorLabels" . | nindent 4 }}
