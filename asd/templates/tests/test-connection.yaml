apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "asd.fullname" . }}-test-connection"
  labels:
    {{- include "asd.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "asd.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
